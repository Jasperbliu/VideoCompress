<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE"
        tools:targetApi="28" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"
        tools:targetApi="34" />

    <!-- Android 11+ 包可见性：用于 ShareHelper.openGallery 直接拉起厂商相册 App -->
    <queries>
        <!-- 通过 Intent 过滤器查询能处理图库浏览的相册 -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="image/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.APP_GALLERY" />
        </intent>
        <!-- 显式声明可查询的厂商相册包名 -->
        <package android:name="com.miui.gallery" />
        <package android:name="com.huawei.photos" />
        <package android:name="com.android.gallery3d" />
        <package android:name="com.coloros.gallery3d" />
        <package android:name="com.oppo.gallery3d" />
        <package android:name="com.vivo.gallery" />
        <package android:name="com.sec.android.gallery3d" />
        <package android:name="com.google.android.apps.photos" />
    </queries>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:roundIcon="@drawable/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.VideoCompress">

        <service
            android:name=".service.CompressForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.VideoCompress">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
